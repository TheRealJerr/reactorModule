#include "../include/Buffer.h"

// 待完善
namespace reactor
{
    uint32_t Buffer::readInt32()
    {
        // 从缓冲区读取一个无符号的整形
        return uint32_t();
    }

    std::string Buffer::readBuffer()
    {
        return std::string();
    }
    
}